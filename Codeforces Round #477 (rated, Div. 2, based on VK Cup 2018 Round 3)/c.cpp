#include<iostream>
#include<cstdlib>
#include<cstdio>
#include<cmath>
#include<cstring>
#include<algorithm>
using namespace std;
const int maxc=1e5+5;
int st[maxn],el[maxn];//stairs,elevators
int main()
{
    std::ios::sync_with_stdio(false);
    cin.tie(0);
    int n,m,cl,ce,v;
    cin>>n>>m>>cl>>ce>>v;
    for(int i=0;i<cl;i++)
        cin>>st[i];
    for(int i=0;i<ce;i++)
        cin>>el[i];
    int q;
    cin>>q;

}
